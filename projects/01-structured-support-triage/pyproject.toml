[project]
name = "structured-support-triage"
version = "0.1.0"
description = "A no-dependency starter project for structured LLM output contracts and evals."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
